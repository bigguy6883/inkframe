# Installed to /etc/NetworkManager/dnsmasq-shared.d/ by install.sh.
# Only used while a NetworkManager shared connection (the inkframe-setup
# hotspot) is active: resolves every DNS name to the hotspot address so
# phones' connectivity probes reach InkFrame and pop the captive portal.
address=/#/10.42.0.1
